{
  "name": "sc-prisma-extractor",
  "version": "0.0.5",
  "description": "A CLI tool to extract your Prisma schema into usable TypeScript interfaces and a detailed metadata JSON file.",
  "main": "dist/index.js",
  "bin": {
    "sc-prisma-extractor": "dist/index.js"
  },
  "scripts": {
    "dev": "nodemon --watch src --watch prisma --ext ts,prisma --exec \"npm run build && npm run start\"",
    "build": "tsc",
    "start": "node dist/index.js ./prisma/schema.prisma ./dev/extracted.ts",
    "validate-config": "node -e \"const { validateConfig, loadConfig } = require('./dist/utils'); try { validateConfig(loadConfig()); console.log('✅ Configuração válida!'); } catch (error) { console.error('🟥', error.message); process.exit(1); }\"",
    "prepublishOnly": "npm run build",
    "deploy": "npm run build && npm version patch && npm publish"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "prisma",
    "generator",
    "typescript",
    "interface",
    "cli",
    "schema",
    "extractor"
  ],
  "author": "saulotarsobc",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saulotarsobc/sc-prisma-extractor.git"
  },
  "bugs": {
    "url": "https://github.com/saulotarsobc/sc-prisma-extractor/issues"
  },
  "homepage": "https://github.com/saulotarsobc/sc-prisma-extractor#readme",
  "license": "ISC",
  "dependencies": {
    "@prisma/generator-helper": "^6.14.0",
    "@prisma/internals": "^6.14.0",
    "commander": "^14.0.0",
    "ora": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.2"
  },
  "contributors": [
    {
      "name": "Saulo Costa",
      "email": "saulotarsobc@example.com",
      "url": "https://github.com/saulotarsobc"
    }
  ]
}
